# Secret-file boundary

Never read, open, print, search, copy, edit, or otherwise access `.env`, `.env.*`, or any file containing environment secrets.

Do not ask the user to paste secret values. Treat the presence of required environment variables as sufficient unless the user explicitly revokes this rule.
